<Application x:Class="PomodoroTrayApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PomodoroTrayApp"
             xmlns:tb="http://www.hardcodet.net/taskbar">
    
    <Application.Resources>
        <ResourceDictionary>
            <tb:TaskbarIcon x:Key="TrayIcon"
                    IconSource="Assets/timer-icon.png"
                    ToolTipText="Pomodoro Timer">
                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Start Pomodoro" Command="{Binding StartCommand}" />
                        <MenuItem Header="Stop" Command="{Binding StopCommand}" />
                        <Separator/>
                        <MenuItem Header="Show" Command="{Binding ShowWindowCommand}" />
                        <MenuItem Header="Exit" Command="{Binding ExitCommand}" />
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
        </ResourceDictionary>
    </Application.Resources>
    
</Application>
